# Contributors

This is a class-project deliverable. Contributions are listed below in
no particular order. Replace the placeholder names with the real ones
before submission.

| Name | Role / responsibility |
|------|------------------------|
| `<Member 1, owner of this README>` | Methodology presenter; design and implementation of the 4-tier router and the Phase 4 v3 z-only stop agent; pipeline integration and end-to-end demos |
| `<Member 2>` | Demo-of-Results presenter; quantitative evaluation of the router and stop agent; results curation under `results/` |
| `<Member 3>` | `<TODO>` |

## How to update this file

1. Edit names and affiliations directly in this Markdown table.
2. Mirror the same names into `CITATION.cff` (root of repo) so any
   downstream citation machinery picks them up automatically.

## Acknowledgements

This project is built on top of the VT-Refine codebase (NVlabs/vt-refine,
CoRL 2025). See `LICENSE` and `docs/upstream_changes.md` for the
relationship between our additions and upstream code.
